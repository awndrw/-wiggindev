{
    "name": "@wiggindev",
    "private": true,
    "packageManager": "yarn@3.2.1",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "yarn workspace @wiggindev/utils run build && yarn workspace @wiggindev/hooks run build",
        "dev": "yarn workspaces foreach run dev",
        "postinstall": "husky install",
        "prepack": "pinst --disable",
        "postpack": "pinst --enable",
        "lint": "tsc --noEmit && eslint .",
        "format": "eslint --fix . && prettier --write .",
        "publish": "yarn workspaces foreach --exclude @wiggindev npm publish --tolerate-republish"
    },
    "devDependencies": {
        "@types/eslint": "^8",
        "@types/prettier": "^2",
        "@types/rimraf": "^3",
        "@typescript-eslint/eslint-plugin": "^5.23.0",
        "@typescript-eslint/parser": "^5.23.0",
        "eslint": "^8.15.0",
        "eslint-config-prettier": "^8.5.0",
        "eslint-plugin-prettier": "^4.0.0",
        "husky": "^8.0.1",
        "lint-staged": "^12.4.1",
        "pinst": "^3.0.0",
        "prettier": "^2.6.2",
        "typescript": "^4.6.4"
    }
}
